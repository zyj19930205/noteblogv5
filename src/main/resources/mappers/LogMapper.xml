<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.wuwenbin.noteblogv5.mapper.LogMapper">

    <select id="ipSummary" resultType="me.wuwenbin.noteblogv5.model.bo.EchartsIpBo">
        SELECT CONCAT(ip_addr, '，', ip_info) AS name, COUNT(*) AS value
        FROM nb_log
        GROUP BY ip_addr, ip_info
    </select>

    <select id="urlSummary" resultType="me.wuwenbin.noteblogv5.model.bo.EchartsUrlBo">
        SELECT DATE_FORMAT(l.time, '%Y-%m-%d') as item, COUNT(*) as cnt
        FROM nb_log l
        WHERE DATE_FORMAT(l.time, '%Y-%m-%d') >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
        GROUP BY DATE_FORMAT(l.time, '%Y-%m-%d')
        ORDER BY l.time DESC
    </select>

    <select id="todayVisit" resultType="long">
        select count(1)
        from nb_log l
        where DATE_FORMAT(l.time, '%Y-%m-%d') = DATE_FORMAT(curdate(), '%Y-%m-%d')
    </select>
</mapper>